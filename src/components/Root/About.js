import React from 'react'
import Menu from './Menu'

const About =() => (
            <div className="container">
                < Menu />
                <div className="text">This is site about cities, click "List if cities" to see them!</div>
            </div>
        )

export default About